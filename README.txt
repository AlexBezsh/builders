To run Maven build:
    - with tests: mvn clean package
    - without tests: mvn clean package -DskipTests
    - only tests: mvn test

To run Gradle build:
    - with tests:
    - without tests:
    - only tests